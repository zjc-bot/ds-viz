# 安全评估报告 — 数据结构可视化学习中心

评估对象:`index.html`(枢纽站)+ `memory_visualizer.html`(可视化工具)
评估日期:2026-08-26
威胁模型:纯前端本地应用,发给同学互相传阅/本地打开,无服务器。

---

## 已实施的安全措施 ✅

| # | 措施 | 说明 |
|---|------|------|
| 1 | **密码不存明文 + 强派生** | PBKDF2-SHA256 **10万次迭代**派生密钥,暴力破解成本提升约10万倍 |
| 1b | **每用户独立随机盐** | 注册时生成128位随机盐(crypto.getRandomValues),同密码不同用户哈希完全不同,彩虹表彻底失效 |
| 1c | **旧哈希自动迁移** | 老版本SHA-256账户在下次成功登录时自动升级为PBKDF2格式,无感过渡 |
| 2 | **用户数据隔离** | 程序存 `mv_programs@<用户名>`,设置存 `ds_settings@<用户名>`;URL 参数传递当前用户,A 同学看不到 B 同学保存的代码 |
| 3 | **会话用 sessionStorage** | 登录态关浏览器即失效,不留长期凭证 |
| 4 | **输入校验** | 用户名白名单正则 `/^[\w\u4e00-\u9fa5]{2,20}$/`、密码≥6位、两次确认 |
| 4b | **登录限速** | 同一用户名5分钟内错满5次 → 锁定60秒,防脚本暴力试密码 |
| 4c | **统一错误文案** | 登录失败只提示"用户名或密码错误",不暴露账号是否存在(防用户名枚举) |
| 4d | **存储结构校验** | 读取用户表时验证每条记录形状,畸形/被篡改数据自动剔除不致崩 |
| 5 | **无 innerHTML 注入面** | 用户名等动态内容均用 `textContent` 渲染,不经 `innerHTML` |
| 6 | **无网络请求** | 零外部依赖(CDN/字体/统计),没有数据外发渠道 |
| 7 | **iframe 天然隔离** | file:// 下跨 frame 访问被浏览器同源策略阻止 |

## 残余风险与已知限制 ⚠️

| # | 风险 | 等级 | 说明与建议 |
|---|------|------|-----------|
| 1 | **纯前端认证可绕过** | 中(教学场景:低) | localStorage 里的哈希可被懂开发者直接改;登录只是"区分数据",不是真鉴权。**这是设计取舍**:无服务器做不到真安全。若需真安全须上后端(如 GitHub Pages + Firebase)。 |
| 2 | ~~盐是静态前缀~~ 已解决 | — | 已升级为每用户128位随机盐+PBKDF2。残余:纯前端无法防"直接改localStorage跳过登录"(见风险1)。 |
| 3 | **XSS 面已最小化但非零** | 低 | 可视化页的代码编辑器把用户 C 代码经 `textContent` 插入 DOM,已防注入;若未来改用 innerHTML 高亮,务必转义 `<>&"` |
| 4 | **localStorage 无加密** | 低 | 保存的 C 代码明文存储。不含敏感信息,可接受。 |
| 5 | **URL 参数未签名** | 低 | 手动改 URL 里 user=xxx 可冒充他人视角(只能看,改不了对方数据,因为写也写到同一键=等于就是那个人)。教学互查场景反而是"特性"。 |

## 结论

**适合当前用途(班级内学习工具分发)。** 密码不明文、数据按人隔离、无注入面、无外联。
不要用于任何需要真实账号体系的场景;如同学间需要真正的隐私边界,升级方案:
1. 部署到 Vercel/GitHub Pages + Supabase/Firebase Auth(免费额度够一个班);
2. 或改为局域网小服务器(Python http.server + 简单 token)。

## 分发建议

把整个文件夹(`index.html` + `memory_visualizer.html`)打包 zip 发给同学,
解压后双击 `index.html` 即可用,无需安装任何东西。
